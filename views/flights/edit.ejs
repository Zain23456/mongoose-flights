<%- include('../partials/html-head')  %> 
<link rel="stylesheet" href="/stylesheets/flights/edit.css" />
<%- include('../partials/nav')  %>

<main>
  <h1><%= title %></h1>
  <form id="new-form" action="/flights/<%= flight._id %>?_method=PUT"" method="post">
    <label for="airline">Airline:</label>
    <select name="airline" id="airline">
      <option value="<%= flight.airline %>"><%= flight.airline %></option>
      <option value="American">American</option>
      <option value="Southwest">Southwest</option>
      <option value="United">United</option>
    </select>

    <label for="airport">Airport:</label>
    <select name="airport" id="airport">
      <option value="<%= flight.airport %>"><%= flight.airport %></option>
      <option value="AUS">AUS</option>
      <option value="DFW">DFW</option>
      <option value="DEN">DEN</option>
      <option value="LAX">LAX</option>
      <option value="SAN">SAN</option>
    </select>

    <label for="flightNo">Flight Number:</label>
    <input name="flightNo" id="flightNo-input" value="<%= flight.flightNo %>">

    <label for="departs-input">Departure Date:</label>
    <input type="datetime-local" id="departs-input" name="departs" value="<%= flight.departs %>">

    <button type="submit">Change Flight</button>
  </form>
</main>

<%- include('../partials/footer')  %> 